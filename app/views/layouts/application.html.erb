<!DOCTYPE html>
<html>
<head>
  <title>Bits of Links</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= auto_discovery_link_tag(:rss, feed_url) %>
  <%= favicon_link_tag "/favicon.png" %>
  <script type="text/javascript" src="//use.typekit.net/ofa3qch.js"></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
</head>
<body>
  <header><h1><%= link_to 'Linky Bits', root_path %>
    <% if @new_item_endabled != false %>
      <span id="newItem"><%= link_to 'New Item', new_item_path if current_user %></span>
    <% end %>
  </h1></header>
  <div id="mainWrap">
    <div id="content">
      <% unless alert.nil? %>
        <p class="alert flash"><%= alert %></p>
      <% end %>
      <% unless notice.nil? %>
        <p class="notice flash"><%= notice %></p>
      <% end %>
    
      <div id="session">
        <% if current_user %>
          <%= link_to 'Post Bookmarklet', '', class: "bookmarklet" %>
          <%= link_to current_user.name, edit_user_path(current_user) %>
          <%= link_to "Log out", logout_path if current_user %>
        <% else %>
          <%= link_to "Log in", login_path if !current_user %>
        <% end %>
      </div>
      <%= yield %>
    </div><!-- #content -->
  </div><!-- #mainWrap -->
  <footer>
    &copy; <%= link_to "Edward Loveall", "http://edwardloveall.com/", target: '_blank' %> <%= Time.now.strftime("%Y")%>
  </footer>
</body>
</html>