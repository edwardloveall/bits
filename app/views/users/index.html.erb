<h1>Users</h1>

<% @users.each do |user| %>
  <div class="user">
    <%= link_to user.username, user, class: "author user-color-" + user.color_id.to_s %>
    <%= link_to 'edit', edit_user_path(user), class: "edit-user" if current_user %>
  </div>
<% end %>