<div class="user-top">
  <span class="user-name"><%= @user.name + "'s bits" %></span>
</div>

<%= render @items %>

<%= paginate @items %>

<% if current_user %>
<div class="bookmarklet-wrap">
  Drag this to your Bookmarks Bar: <%= link_to 'Post', '', class: "bookmarklet" %>
</div>

<% end %>